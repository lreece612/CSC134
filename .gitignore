# this is specific to the CSC134 repo, and should be 
# used to ignore the obvious things

# Powerpoints and word documents (since I only use open documents)

*.ppt
*.doc
*.docx


# gedit likes to save ~ files. Don't need those.
*~


# also ignore compiled output, including pdfs
*.out
*.pdf

# ignore zips
*.zip
